/**
 * Events shared across application
 * @module Events
 * @preferred
 */

const APP_GLOBAL_EVENTS = {
    events: {
        EVT_SET_FORM_DATA: 'EVT_SET_FORM_DATA'
    }
};
export const AppConstants = Object.freeze(APP_GLOBAL_EVENTS);
